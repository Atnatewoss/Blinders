# Client

Next.js + Tailwind UI for the AI Fantasy League Commissioner.

## Run

```bash
npm install
npm run dev
```

The client expects the FastAPI server at `http://127.0.0.1:8000`. Override with:

```bash
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000 npm run dev
```
